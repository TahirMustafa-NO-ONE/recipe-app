# =============================================================================
# Moreat Recipe Book - Environment Variables Template
# =============================================================================
# INSTRUCTIONS:
#   1. Copy this file and rename the copy to ".env"
#   2. Fill in the actual values for your environment
#   3. NEVER commit the .env file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Spoonacular Recipe API
# -----------------------------------------------------------------------------
# Get your free API key at: https://spoonacular.com/food-api
# Used for recipe search, filtering, and detail fetching
SPOONACULAR_API_KEY=your_spoonacular_api_key_here

# -----------------------------------------------------------------------------
# Firebase Configuration (Android)
# -----------------------------------------------------------------------------
# Obtain from Firebase Console > Project Settings > Your Apps > Android app
# https://console.firebase.google.com/
FIREBASE_PROJECT_NUMBER=your_firebase_project_number
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app
FIREBASE_APP_ID_ANDROID=1:your_project_number:android:your_app_hash
FIREBASE_API_KEY_ANDROID=your_android_firebase_api_key

# -----------------------------------------------------------------------------
# Google OAuth Client IDs (Android)
# -----------------------------------------------------------------------------
# Obtained from Firebase Console > Authentication > Sign-in method > Google
# or from Google Cloud Console > APIs & Services > Credentials
GOOGLE_OAUTH_CLIENT_ID_ANDROID=your_android_oauth_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_ID_WEB=your_web_oauth_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CERTIFICATE_HASH=your_sha1_certificate_fingerprint
